const puppeteer = require('puppeteer');(async () => {const browser = await puppeteer.launch({ headless: 'new' });const page = await browser.newPage();await page.goto('https://pengumuman-snbp.snpmb.id/?renderer=html', { waitUntil: 'networkidle2' });await new Promise(r=>setTimeout(r,1000));await page.type('#index-form-registration-number', '426631796');await page.type('#index-form-birthday-day', '15');await page.type('#index-form-birthday-month', '04');await page.type('#index-form-birthday-year', '2008');await page.click('#index-form-submit');await new Promise(r=>setTimeout(r,2000));const page2 = await browser.newPage();let intercepted = false;page2.on('request', req => { if(req.url().includes('.json') && !req.url().includes('config')) intercepted = req.url(); });await page2.goto('https://pengumuman-snbp.snpmb.id/?renderer=html', { waitUntil: 'networkidle2' });await new Promise(r=>setTimeout(r,1000));console.log('Intercepted on load:', intercepted);await browser.close();})();
