const puppeteer = require('puppeteer');(async () => {const b = await puppeteer.launch({ headless: 'new' });const p = await b.newPage();await p.goto('https://pengumuman-snbp.snpmb.id/?renderer=html', { waitUntil: 'networkidle2' });await new Promise(r => setTimeout(r, 2000));const html = await p.content();console.log(html.includes('index-form-registration-number') ? 'FOUND INPUT' : 'NOT FOUND');await b.close();})();
