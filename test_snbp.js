const puppeteer = require('puppeteer');(async () => {  const browser = await puppeteer.launch();  const page = await browser.newPage();  page.on('request', r => { const u = r.url(); if(u.includes('.json')||u.includes('.dwg')) console.log('REQ:', u); });  await page.goto('https://snbp.ui.ac.id/', { waitUntil: 'networkidle2' });  await new Promise(r => setTimeout(r, 2000));  await page.mouse.click(500, 548);  await page.keyboard.type('426630866');  await page.mouse.click(293, 744); await page.keyboard.type('12'); await page.mouse.click(402, 744); await page.keyboard.type('12'); await page.mouse.click(550, 744); await page.keyboard.type('2006'); await page.mouse.click(500, 800); await page.keyboard.press('Enter');  await new Promise(r => setTimeout(r, 5000));  await browser.close();})();
