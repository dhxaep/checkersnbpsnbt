const crypto = require('crypto'); const target = '42663086620061212'; const hashes = ['md5', 'sha1', 'sha256', 'sha512']; for (let h1 of hashes) { for (let h2 of hashes) { const step1 = crypto.createHash(h1).update(target).digest(); const step2 = crypto.createHash(h2).update(step1).digest('hex'); if (step2.endsWith('01981f')) { console.log('BINGO BINARY:', h1, h2); } const step1Hex = crypto.createHash(h1).update(target).digest('hex'); const step2Hex = crypto.createHash(h2).update(step1Hex).digest('hex'); if (step2Hex.endsWith('01981f')) { console.log('BINGO HEX:', h1, h2); } } } console.log('Done');
