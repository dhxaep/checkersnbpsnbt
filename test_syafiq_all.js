const puppeteer = require('puppeteer');(async () => {const browser = await puppeteer.launch({ headless: 'new' });const page = await browser.newPage();page.on('request', (req) => {const url = req.url();if (url.includes('.json')) console.log('REQ:', url);});await page.goto('https://pengumuman-snbp.snpmb.id/?renderer=html', { waitUntil: 'networkidle2' });await new Promise(r => setTimeout(r, 1000));await page.type('#index-form-registration-number', '426214878');await page.type('#index-form-birthday-day', '01');await page.type('#index-form-birthday-month', '01');await page.type('#index-form-birthday-year', '2005');await page.click('#index-form-submit');await new Promise(r => setTimeout(r, 3000));await browser.close();})();
